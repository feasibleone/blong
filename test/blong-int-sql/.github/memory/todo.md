# Todo — test/blong-int-sql

Work that is deferred, unfinished, or spotted and not done yet. Entries here concern
`test/blong-int-sql`; anything cross-cutting belongs in the repository-root memory.

<!-- memory:index -->
<!-- generated by `blong-dev memory index` — do not edit -->

open (2)

- `T-065` · test/blong-int-sql — Whole-transaction retry is missing for transient connection errors
- `T-066` · test/blong-int-sql — `knex.retry` is not enabled for `blong-int-sql`'s own knex adapter

<!-- /memory:index -->

## Open

### T-065 — Whole-transaction retry is missing for transient connection errors

> _2026-09-07 · test/blong-int-sql · open_

Version 1 of the connection retry only retries builder and raw queries; transactions surface the
error. Consider re-invoking the transaction callback on retryable errors — knex rolls back on throw
— which needs care about external side effects.

### T-066 — `knex.retry` is not enabled for `blong-int-sql`'s own knex adapter

> _2026-08-21 · test/blong-int-sql · open_

Enable `knex.retry` for `blong-int-sql`'s own `extends: 'adapter.knex'` adapter
(`core/blong-int-sql/mysql/adapter/sql.ts`). Currently only the shared `srv.db` adapter enables it.
