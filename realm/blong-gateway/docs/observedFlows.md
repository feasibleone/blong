# Sequence Diagrams

```mermaid
sequenceDiagram
    autonumber
    participant blong
    participant gateway
    blong->>gateway: gateway.gateway.bundle.find
    gateway-->>blong: gateway.gateway.bundle.find
```
