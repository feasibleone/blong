# Todo — realm/blong-gateway

Work that is deferred, unfinished, or spotted and not done yet. Entries here concern
`realm/blong-gateway`; anything cross-cutting belongs in the repository-root memory.

<!-- memory:index -->
<!-- generated by `blong-dev memory index` — do not edit -->

open (1)

- `T-147` · realm/blong-gateway — The gateway cleanup dialog baselines carry whatever the database
  held

<!-- /memory:index -->

## Open

### T-147 — The gateway cleanup dialog baselines carry whatever the database held

> _2026-09-24 · realm/blong-gateway · open_

Sweeping the baselines showed gateway-bundle-cleanup-confirm (0.47% of the shot: the dialog list
rows) and gateway-application-cleanup-confirm (0.06%) differing from their baselines in the list the
dialog shows, not in the chrome the sweep was about. The dialog lists live rows, so the baseline
records the day it was written rather than a state the shot can reproduce. Filter the dialog to a
seeded row, or mask its list, the way the commander explore shots narrow every source to a stable
subset.
