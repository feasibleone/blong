# Todo — realm/blong-access

Work that is deferred, unfinished, or spotted and not done yet. Entries here concern
`realm/blong-access`; anything cross-cutting belongs in the repository-root memory.

<!-- memory:index -->
<!-- generated by `blong-dev memory index` — do not edit -->

open (1)

- `T-075` · realm/blong-access — `findUsers` in `testAccessModelFlow` breaks past 100 access users

<!-- /memory:index -->

## Open

### T-075 — `findUsers` in `testAccessModelFlow` breaks past 100 access users

> _2026-09-12 · realm/blong-access · open_

`realm/blong-access/server/test/test/testAccessModelFlow.ts` `findUsers`, around line 186, does the
unfiltered `accessUserFind({paging: {pageNumber: 1, pageSize: 100}})` followed by
`result.some(item => item.userId === created.userId)`. `access_user` grows with every run, so this
breaks once it passes 100 rows — the same latent bug class as the kukum e2e fixture's `widget find`.
The fix is the same: the `addUser` step already mints `model-test-${Date.now()}@example.com`, so
thread that email through the chain and use `filterBy: {emailAddress}`. Left undone deliberately,
because it needs a `blong-access` run (and therefore backends) to verify and it sits in a
hand-written realm test rather than a shipped template. `demo/blong-marine/meta/model/*.ts` also
uses `pageSize: 100`, but for `listParams` feeding a dropdown, which is a different and legitimate
use.
