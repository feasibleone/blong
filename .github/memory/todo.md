# Todo

Work that is deferred, unfinished, or spotted and not done yet. Cross-cutting entries only: anything
specific to one package belongs in that package's `.github/memory/todo.md`.

<!-- memory:index -->
<!-- generated by `blong-dev memory index` — do not edit -->

open (31)

- `T-047` · ci — `test/blong-ci-report` reserves Playwright ports nothing uses yet
- `T-048` · ci — `wanples` and `release-cd` define no `ci-test`, so no report rows
- `T-049` · ci — `core/blong-chain` masks tap failures and had flaky duration assertions
- `T-050` · ci — A bare `blong-dev test` picks up `core/blong-chain`'s failing examples
- `T-051` · ci — `realm/blong-access` has a genuinely flaky Playwright cleanup test
- `T-052` · ci — `ci-report history` writes its rebuilt files wherever `--out` points
- `T-053` · ci — `update-metrics.yaml` is unused by blong and can be deleted
- `T-055` · ci — JUnit XML emission was dropped with the EnricoMi step
- `T-056` · ci — MySQL CI settings are left at defaults pending diagnostics
- `T-057` · ci — wanples CI contention is out of scope for the retry workaround
- `T-058` · cross-cutting — Test runs skipped after the folder restructure, for lack of backends
- `T-059` · ci — `rush ci-coverage` was never run end-to-end
- `T-060` · cross-cutting — `rush.json` `tags` still say `core` for moved packages
- `T-061` · cross-cutting — `ext/rest-fs` could move under `tools/` once the CI globs land
- `T-062` · cross-cutting — Backend state is ephemeral for minio, redis, kafka and vault
- `T-076` · skills — The handler file-name rule is not a repo-wide convention yet
- `T-077` · skills — The file-name rule still needs promoting from kukum to the skills
- `T-078` · docs — The full docusaurus build of `docs/blong` was never run
- `T-079` · cross-cutting — A component's `cli` log level is still per-component boilerplate
- `T-080` · cross-cutting — Maintenance commands still hand-roll their own argv handling
- `T-081` · skills — `patterns/cli.md` is unlinked and the `blong-intent` table omits `cli`
- `T-082` · cross-cutting — `release-please-config.json` gained a `core/semantic-log` entry
- `T-083` · docs — One pre-existing broken anchor in the `docs/blong` build
- `T-090` · ci — A leftover approval-pending run on pull request #172
- `T-054` · ci — A missing `report-src` degrades CI quietly with only a warning
- `T-092` · skills — The prose validator never checks verdict completeness
- `T-093` · ci — The stacked metrics pull request must be merged by hand
- `T-097` · docs — Document the ACL feature
- `T-101` · ci — No test seeds an empty database
- `T-115` · ci — Four packages fail their own ci-test today
- `T-122` · cross-cutting — The other realms' page captures mask variable-width cells too

<!-- /memory:index -->

## Manual

- todos must be written as instructions
- gate the console calls in the browser
- agents struggle with coverage, create a tool/skill for tap - add screenshots and diagrams to the
  docs - agents frequently cause bash error `event not found` when they run `grep` with a pattern
  containing `!` (e.g. `!route`) Example:

    ```bash
    cd /home/kalin/work/blong/blong/core/semantic-log && echo "=== hubA.ts ===";
    grep -n "localRate\|localStatus\|crossed\|withhold\|settled.status\|const local" flow/hubA.ts;
    echo "=== proxy.ts ===";
    grep -n "reachable\|decide\|route\|if (!route" flow/proxy.ts;
    echo "=== flows.ts ===";
    grep -n "basePort\|level: options.level\|retries\|participants.push\|kind === 'single'" flow/flows.ts;
    echo "=== hub.ts ===";
    grep -n "?? 1\|?? 'unknown'\|withhold" flow/hub.ts;
    echo "=== payer.ts ===";
    grep -n "?? 1\|rateLimit =\|quote.body" flow/payer.ts
    ```

- lib unit tests - allow easy testing of library() functions
- skills as tools
- flow diagram for method calls
- component diagram for a suite
- https://github.com/tt-a1i/archify
- https://github.com/trailhq/Graft
- time bound debug tokens
- match and mask
- transient form fields
- table widget dual role / no singleton
- stable keys
- remove axios
- playwright test runner for backend tests
- telemetry
- blong-kustomize
- same queries are repeated in multiple places, they should be refactored into a single function
- avatar photo upload (initials-only for now — per user decision)
- allow multi statement in blong-dev sql
- create db admin ui
- create k8s admin ui
- tests are doing too many assertions instead of snapshotting
- compile queries to procedures
- combined storybook
- backend for the storybook
- translations from the DB
- report hanging handlers after tests

## Open

### T-047 — `test/blong-ci-report` reserves Playwright ports nothing uses yet

> _2026-09-15 · ci · open_

`test/blong-ci-report` is registered as the 45th project in `rush.json`, which reserves CI ports
9045/9145 for it. Playwright ports are derived from each package's index in the `projects` array,
and nothing uses Playwright in that package yet, so the ports are only reserved. Reorder that array
only with the port derivation in mind.

### T-048 — `wanples` and `release-cd` define no `ci-test`, so no report rows

> _2026-09-15 · ci · open_

`wanples` and `release-cd` call `rush.yaml` but define no `ci-test` command (they use `ci-unit`), so
their `Run Tests` step fails and they produce no report rows. They need a `ci-test` script to adopt
the `.ci-report/` contract, and a `ci-report` script to get the aggregate.

### T-049 — `core/blong-chain` masks tap failures and had flaky duration assertions

> _2026-09-09 · ci · open_

`core/blong-chain`'s `ci-test` still masks its tap failures with `;` before `./test-examples-ci.sh`.
Its duration assertions were flaky: Node timers can fire about 1 ms early, so `duration >= 50` for a
50 ms sleep failed roughly once in 60 runs. That is fixed with a documented `TIMER_SLACK_MS`,
verified over 16 consecutive runs, six of them under CPU load. Whether the step should fail the job
is still open.

### T-050 — A bare `blong-dev test` picks up `core/blong-chain`'s failing examples

> _2026-09-09 · ci · open_

A bare `blong-dev test` in `core/blong-chain` also picks up `examples/*.test.ts`, which fail by
design; `ci-test` passes the glob explicitly and `TESTING.md` now warns about it. Consider making
the runner's default skip `examples/`, or leave it as documentation.

### T-051 — `realm/blong-access` has a genuinely flaky Playwright cleanup test

> _2026-09-09 · ci · open_

`user.play.ts › Access User › cleanup access user` in `realm/blong-access` is genuinely flaky: it
fails roughly once per run and passes on retry. The new CI report surfaces it as flaky with its
trace, so it is no longer invisible — decide whether to fix the test or accept the flake.

### T-052 — `ci-report history` writes its rebuilt files wherever `--out` points

> _2026-08-21 · ci · open_

`ci-report history` writes its rebuilt files where `--out` points, which is the repository root in
CI. The first CI run should confirm that `commit-metrics` produces exactly one commit per run.

### T-053 — `update-metrics.yaml` is unused by blong and can be deleted

> _2026-08-21 · ci · open_

`update-metrics.yaml` is now unused by blong: it is kept as a reusable workflow for other
repositories. Delete it if nothing adopts it.

### T-055 — JUnit XML emission was dropped with the EnricoMi step

> _2026-08-21 · ci · open_

JUnit XML emission was deliberately dropped with the EnricoMi step. Reviving per-test check-run
annotations means new work, not a restore.

### T-056 — MySQL CI settings are left at defaults pending diagnostics

> _2026-08-21 · ci · open_

This is a follow-up on the MySQL CI settings, pending diagnostics from the `ci-diagnostics`
artifact: bump the memory limits in `test/integration/mysql-deployment.yaml` if the pod is
OOMKilled, add `--max_connections` if `ER_CON_COUNT_ERROR` (1040) shows up, and tweak `wait_timeout`
only if idle-close is confirmed. The settings are currently left at MySQL defaults.

### T-057 — wanples CI contention is out of scope for the retry workaround

> _2026-09-07 · ci · open_

wanples CI runs with maximum parallelism and therefore higher contention. That is intentionally out
of scope for the retry workaround: revisit if it starts hitting the same connection drops.

### T-058 — Test runs skipped after the folder restructure, for lack of backends

> _2026-09-10 · cross-cutting · open_

The backends were not running in that session, so these test runs were skipped: `test/framework`
(`@feasibleone/test`), `test/blong-int-sql`, `test/blong-int-adapter`, `realm/blong-access`,
`realm/blong-party`, `realm/blong-gateway`, `realm/blong-commander`, `realm/blong-test`,
`demo/blong-marine`, `suite/blong-suite` and `tools/blong-ttk` — all of them need MySQL (3306)
and/or browser backends. Verified in their place: `core/blong-gogo` (including the graceful-shutdown
spawn test against `demo/blong-hello`), `demo/blong-eip` (23/23), `rush build` and `rush ci-lint`;
the scaffolder and `copy-template.mjs` template resolution were smoke-tested.

### T-059 — `rush ci-coverage` was never run end-to-end

> _2026-09-10 · ci · open_

`rush ci-coverage` was not run end-to-end, because it needs `ci-test` artifacts first, so
`run-coverage.sh`'s new `pkg_path` category map was validated by path existence only.

### T-060 — `rush.json` `tags` still say `core` for moved packages

> _2026-09-10 · cross-cutting · open_

The `tags` in `rush.json` still say `core` for packages now under `realm/`, `suite/`, `demo/`,
`test/` and `tools/`. That was left unchanged deliberately, because nothing filters on `tag:*`
today, but it is worth aligning with the new categories.

### T-061 — `ext/rest-fs` could move under `tools/` once the CI globs land

> _2026-09-10 · cross-cutting · open_

Consider moving `ext/rest-fs` to `tools/rest-fs` once the widened `infitx-org/actions` CI globs are
merged to `main` and other consumers no longer rely on `ext/`.

### T-062 — Backend state is ephemeral for minio, redis, kafka and vault

> _2026-09-10 · cross-cutting · open_

The `minio`, `redis`, `kafka` and `vault` backends have no `volumeMounts` or `persistentVolumeClaim`
in their `test/integration/*.yaml` manifests — unlike mysql, mongodb and keycloak, which have PVCs.
Any cluster restart therefore wipes their data silently while the corresponding one-shot init Jobs
stay `Complete`, so they never re-seed.

Mitigated locally: `~/.local/bin/k3d-up` now deletes all init Jobs, re-applies `test/integration/`,
waits for them and ensures the `blong-<suite>-<user>` MySQL databases, so a host restart self-heals.
Every init Job is idempotent as of that change. CI is unaffected because it always starts a fresh
k3d cluster. Remaining candidates, not implemented: add a PVC to `minio-deployment.yaml` /
`kafka-deployment.yaml` so restarts preserve the data outright; or make the s3 test create its own
bucket, which was rejected for now because the `commander/hello.txt` object is intentionally
provisioned by k8s so that Playwright screenshots stay deterministic.

### T-076 — The handler file-name rule is not a repo-wide convention yet

> _2026-09-12 · skills · open_

The file-name rule — the file name must match the API endpoint path, skip the segment that differs
when one file serves several paths, and never double-book a name — is applied in
`core/blong-kukum/orchestrator/kukum/` but is not written down in
`.github/skills/_shared/conventions.md` or the `blong-handler` skill. That was left out on purpose,
because shared framework docs affect other realms; confirm before documenting it as a general
convention.

### T-077 — The file-name rule still needs promoting from kukum to the skills

> _2026-09-12 · skills · open_

The file-name rule is now documented for kukum users in `docs/blong/docs/patterns/kukum.md`. It is
still not stated in `.github/skills/_shared/conventions.md` or the `blong-handler` skill as a
repo-wide convention, so that promotion is still open.

### T-078 — The full docusaurus build of `docs/blong` was never run

> _2026-09-12 · docs · open_

The full `docusaurus build` in `docs/blong` was not run during the kukum docs work, so the new page
is only reviewed rather than built.

### T-079 — A component's `cli` log level is still per-component boilerplate

> _2026-09-12 · cross-cutting · open_

A component's `cli: {logLevel: 'warn'}` is still per-component boilerplate. Moving it would mean the
framework injecting a default log level into every loaded component under the `cli` intent, and
there is no hook for that today: `AdapterBase` falls back to `'info'` when the component's own
`config.logLevel` is unset. Worth doing if a third command-driven realm appears.

### T-080 — Maintenance commands still hand-roll their own argv handling

> _2026-09-12 · cross-cutting · open_

`bin/fixture.ts`-style maintenance commands and `tools/blong-dev` still hand-roll their own argv
handling; `runCli` may fit them too.

### T-081 — `patterns/cli.md` is unlinked and the `blong-intent` table omits `cli`

> _2026-09-12 · skills · open_

`patterns/cli.md` is new and unlinked from the intent docs beyond the note in `concepts/intents.md`,
and the `blong-intent` skill's layer-activation table still omits `cli`.

### T-082 — `release-please-config.json` gained a `core/semantic-log` entry

> _2026-09-14 · cross-cutting · open_

`release-please-config.json` gained `"core/semantic-log": {}` during that session, most likely from
`rush update` syncing the release config. It is not part of the task and was not reverted, and it is
flagged here so whoever commits can decide.

### T-083 — One pre-existing broken anchor in the `docs/blong` build

> _2026-09-14 · docs · open_

The `docs/blong` build reports one pre-existing broken anchor, `../concepts/adapter#database`,
coming from `patterns/handler`. It is unrelated to the semantic-log work and was left alone.

### T-090 — A leftover approval-pending run on pull request #172

> _2026-09-15 · ci · open_

Pull request #172 still shows one run awaiting approval (`34997062789`) from the last metrics commit
made before `[skip ci]`; dismiss it. Metrics commits no longer start a run.

### T-054 — A missing `report-src` degrades CI quietly with only a warning

> _2026-08-21 · ci · open_

A missing `report-src` only produces a `::warning:: report source not found` in `deploy-report`, so
a regression in the `reports` artifact upload degrades quietly - `include-hidden-files: true` is
what keeps `.ci-report` in that artifact. The links inside the report are likewise _predicted_ from
the reports base URL rather than collected from the publish jobs, so a failed publish leaves a dead
link behind a report that still reads as complete. Consider failing the publish job instead, once
the hidden-file fix has proven itself in CI.

### T-092 — The prose validator never checks verdict completeness

> _2026-09-15 · skills · open_

analyze-prose.ts validateFindings validates every verdict it receives (known segmentId, known
category, verbatim quote) but never checks that each segment of the chunk received one, although the
prose-anomaly prompt demands an entry for EVERY segment id and session-analyser.ts claims 'no silent
failures'. A model that omits a segment therefore passes unnoticed. The log-embed-prd run reported 0
findings, 0 rejected and 7640 explicit normal verdicts for 7641 segments — only that one-segment
arithmetic gap exposed the omission; a parseable reply with several omissions would look identical
to a clean session. Detect it by comparing chunk.segments.length against findings + normalVerdicts +
rejected and surfacing the shortfall in the coverage block.

### T-093 — The stacked metrics pull request must be merged by hand

> _2026-09-16 · ci · open_

The stacked pull request is deliberately not auto-merged: workflows are left off for the stacked
branch for now (a stacked pull request may not spend a CI run on two generated files), so nothing
can merge it and enabling auto-merge would first mean deciding what is allowed to run for a
metrics-only change. Until then the baseline reaches main only when a human merges the stacked pull
request; an unmerged one is rebuilt on every run and the same pull request is refreshed, so nothing
is lost.

### T-097 — Document the ACL feature

> _2026-09-16 · docs · open_

Missing docs: realm/blong-access/README.md (ACL section: the access_acl shape, implicit vs explicit,
deny-wins, the unscoped-records rule, verify), .github/skills/blong-core/SKILL.md (authorization
chapter), .github/skills/blong-schema/SKILL.md (ISchemaTable.acl and resource.nameColumn),
.github/skills/blong-model/SKILL.md (pivot defaults and tri-state cells),
docs/blong/docs/concepts/sessions.md (the verify action/record contract), and a mention in
.github/copilot-instructions.md.

### T-101 — No test seeds an empty database

> _2026-09-16 · ci · open_

The fresh-database outcome of role-bit allocation is reasoned and partly verified rather than
executed: the allocator starts at 0 (MAX on an empty set is null - checked directly in SQL) and the
seed merge allocates in file order (covered by a tap step), but nothing runs the db intent against
an empty schema end to end because it needs the suite harness. A CI job that seeds a throwaway
database and asserts the seeded role bits would close it.

### T-115 — Four packages fail their own ci-test today

> _2026-09-19 · ci · open_

Observed while verifying the new test scripts; each fails identically through its existing ci-test,
so it is not caused by them: core/blong-kukum (2 tap failures - its e2e Playwright leg exits
non-zero and the added entity is not browsed), demo/blong-cli (9), test/framework
(nscfg/index.test.ts exits 1), test/blong-int-adapter (all 9 fail, although all seven backend ports
were listening). Worth a pass each: either the tests or the environment they assume.

### T-122 — The other realms' page captures mask variable-width cells too

> _2026-09-22 · cross-cutting · open_

F-228 was the digest cell of the framework realm, and the same shape exists wherever a spec masks a
value whose width varies - minted ids, timestamps, etags - in the page captures of the other realms
(blong-access, blong-party, blong-marine, blong-commander and the suite). They pass today because
each masked value happens to be close in width to the baseline's, which is the same luck the digest
had for a day. Fix as they surface, by the rule in D-250 (give the masked element a fixed box), or
sweep them deliberately with a run of each suite after a baseline regeneration.
