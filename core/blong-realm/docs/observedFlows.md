# Sequence Diagrams

```mermaid
sequenceDiagram
    autonumber
    participant gateway
    participant blong
    gateway->>blong: gateway.blong.flow.find
    blong-->>gateway: gateway.blong.flow.find
```
