import {Meta, ArgTypes, Canvas, Story} from '@storybook/blocks';
import * as Stories from '../../stories/AdvancedPatterns.stories';

<Meta of={Stories} />

# PivotTable

Pivot table component supporting static and dynamic pivot configurations.

## Usage

<Canvas of={Stories.StaticPivot} />

## Props

<ArgTypes of={Stories} />

## Design Notes

Use StaticPivot for predefined rows; DynamicPivot pulls rows from dropdown data.
