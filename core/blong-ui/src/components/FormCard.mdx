import {Meta, ArgTypes, Canvas, Story} from '@storybook/addon-docs/blocks';
import * as Stories from '../../stories/FormCard.stories';

<Meta of={Stories} />

# FormCard

Renders a form from a JSON schema with card grouping and layout. Supports create and edit modes. Integrates with react-hook-form.

## Usage

<Canvas of={Stories.CreateMode} />

## Props

<ArgTypes of={Stories} />

## Design Notes

Use with FormFactory internally. Supports dropdowns, custom widgets, tabbed layouts.
