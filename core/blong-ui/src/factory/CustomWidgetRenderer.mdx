import {Meta, ArgTypes, Canvas, Story} from '@storybook/blocks';
import * as Stories from '../../../stories/CustomWidgetRenderer.stories';

<Meta of={Stories} />

# CustomWidgetRenderer

Escape hatch for custom React widget components. Receives Input, Label, ErrorLabel internals as props.

## Usage

<Canvas of={Stories.Default} />

## Props

<ArgTypes of={Stories} />

## Design Notes

Register custom widgets in the editors map. The properties array declares which form fields the widget manages.
