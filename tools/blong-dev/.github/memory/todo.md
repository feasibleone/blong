# Todo — tools/blong-dev

Work that is deferred, unfinished, or spotted and not done yet. Entries here concern
`tools/blong-dev`; anything cross-cutting belongs in the repository-root memory.

<!-- memory:index -->
<!-- generated by `blong-dev memory index` — do not edit -->

open (5)

- `T-002` · tools/blong-dev — Migrate the three root memory files
- `T-038` · tools/blong-dev — `bin/fixture.ts`-style maintenance commands and `tools/blong-dev`
  still
- `T-046` · tools/blong-dev — Demote headings embedded in migrated entry bodies
- `T-151` · tools/blong-dev — Memory bodies silently flatten bullet lists
- `T-152` · tools/blong-dev — Playwright failures have no location in the report

<!-- /memory:index -->

## Open

### T-002 — Migrate the three root memory files

> _2026-09-15 · tools/blong-dev · open_

todo.md first (smallest, proves the format), then friction.md (about 110 entries, roughly 65 moving
to packages), then decision.md in a single commit after an approved dry run (the wood theme block
collapses to the five items the blong-theme skill does not already record). Then the prune pass,
then check --all must be green.

### T-038 — `bin/fixture.ts`-style maintenance commands and `tools/blong-dev` still

> _2026-09-12 · tools/blong-dev · open_

hand-roll their own argv handling; `runCli` may fit them too.

### T-046 — Demote headings embedded in migrated entry bodies

> _2026-09-15 · tools/blong-dev · open_

A migrated body can still contain a '## ' line (decision.md reports unknown section '## Resolved'),
which the parser reads as a section. The migrator should demote such lines (bold or plain) when it
captures a body, and check should keep reporting them until it does.

### T-151 — Memory bodies silently flatten bullet lists

> _2026-09-26 · tools/blong-dev · open_

A body handed to `memory add --body-file` (and `memory edit --body-file`) loses its bullet lists:
the items are joined into one paragraph with the `- ` markers left inside the text, which then fails
`memory check` with MD031 (a list needs a blank line below it). Measured on D-279 and F-269.

Two ways out, either is fine: teach the writer to keep a list's structure (blank line between
blocks, marker on its own line) so a body may be written as prose or as bullets, or say in the
instructions that a body is prose only, so nobody writes a list and then discovers MD031.

### T-152 — Playwright failures have no location in the report

> _2026-09-26 · tools/blong-dev · open_

A browser-leg failure reaches the aggregate report and the failures bundle with no file and line, so
its Location cell reads — and the failure table cannot point a reader at the spec that failed. The
data is there: the Allure result a Playwright run leaves in allure-results carries a package label
that names the spec (@feasibleone/blong-access.profile.play.ts in the committed history) and a name
label with the test title, so parseResults should read it and set the test entry's file and line.
Two things improve once it does: the report can show where a browser failure is, and provenance can
disambiguate a test name that several specs of one package share, which today falls back to unknown.
The tap leg already has locations, because tap reports them in its own diagnostics, so this is only
the browser leg.
