{
    "name": "rest-fs",
    "displayName": "REST Filesystem",
    "version": "1.4.2",
    "description": "A filesystem provider backed by REST API",
    "categories": [
        "Other"
    ],
    "repository": {
        "url": "git+https://github.com/feasibleone/blong.git"
    },
    "license": "BlueOak-1.0.0",
    "publisher": "undertree",
    "main": "./dist/extension.js",
    "scripts": {
        "build": "npm run compile",
        "check-types": "tsc --noEmit",
        "ci-lint": "npm run lint",
        "ci-test": "xvfb-run -a npm run test",
        "compile": "npm run check-types && npm run lint && node esbuild.js",
        "compile-tests": "tsc -p . --outDir out",
        "lint": "eslint src",
        "package": "npm run check-types && npm run lint && node esbuild.js --production",
        "pretest": "npm run compile-tests && npm run compile && npm run lint",
        "test": "vscode-test",
        "vscode:prepublish": "npm run package",
        "watch": "npm-run-all -p watch:*",
        "watch:esbuild": "node esbuild.js --watch",
        "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
        "watch-tests": "tsc -p . -w --outDir out"
    },
    "contributes": {
        "commands": [
            {
                "command": "restfs.openWorkspace",
                "title": "REST FS: Open Workspace"
            },
            {
                "command": "restfs.addWorkspace",
                "title": "REST FS: Add Workspace"
            },
            {
                "command": "restfs.configure",
                "title": "REST FS: Configure API Endpoint"
            }
        ],
        "configuration": {
            "title": "REST Filesystem",
            "properties": {
                "restfs.blongLogCachePath": {
                    "type": "string",
                    "default": "~/.blong/log-cache",
                    "description": "Path to the cacache directory used by the Blong pino-cacache transport. Used to look up log entries when clicking blong://log/ links in the terminal."
                },
                "workspace": {
                    "type": "object",
                    "description": "Configuration for REST API filesystem workspaces",
                    "additionalProperties": {
                        "type": "object",
                        "properties": {
                            "baseUrl": {
                                "type": "string",
                                "default": "http://localhost:3000/api/fs",
                                "description": "Base URL for the REST API filesystem backend"
                            },
                            "headers": {
                                "type": "object",
                                "default": {
                                    "Authorization": "Basic base64-encoded-credentials"
                                },
                                "description": "Custom headers to send with each REST API request (e.g., authentication tokens)"
                            }
                        }
                    },
                    "default": {}
                }
            }
        },
        "taskDefinitions": [
            {
                "type": "restfs-shell",
                "required": [
                    "workspace",
                    "command"
                ],
                "properties": {
                    "workspace": {
                        "type": "string",
                        "description": "Name of the REST FS workspace to execute the shell command in"
                    },
                    "command": {
                        "type": "string",
                        "description": "Shell command to execute on the REST backend"
                    },
                    "cwd": {
                        "type": "string",
                        "description": "Optional working directory for the shell command"
                    }
                }
            }
        ]
    },
    "activationEvents": [
        "onFileSystem:restfs",
        "onFileSystemAccess:restfs",
        "onUri"
    ],
    "dependencies": {
        "cacache": "^20.0.4",
        "ws": "^8.18.0"
    },
    "devDependencies": {
        "@types/cacache": "^20.0.1",
        "@types/mocha": "^10.0.10",
        "@types/node": "^24",
        "@types/vscode": "^1.107.0",
        "@types/ws": "^8.18.0",
        "@vscode/test-cli": "^0.0.12",
        "@vscode/test-electron": "^2.5.2",
        "esbuild": "^0.27.1",
        "eslint": "^10.4.0",
        "npm-run-all": "^4.1.5",
        "typescript": "^6.0.3",
        "typescript-eslint": "^8.59.0"
    },
    "engines": {
        "vscode": "^1.107.0"
    }
}
