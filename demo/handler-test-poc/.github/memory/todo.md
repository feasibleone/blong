# Todo — demo/handler-test-poc

Work that is deferred, unfinished, or spotted and not done yet. Entries here concern
`demo/handler-test-poc`; anything cross-cutting belongs in the repository-root memory.

<!-- memory:index -->
<!-- generated by `blong-dev memory index` — do not edit -->

open (1)

- `T-135` · demo/handler-test-poc — The demo alt-and-note test is blocked by the missing test entry

<!-- /memory:index -->

## Open

### T-135 — The demo alt-and-note test is blocked by the missing test entry

> _2026-09-23 · demo/handler-test-poc · open_

The plan wanted a demo test asserting the alt block and the note end to end for a graduated handler.
It is blocked by the package having no `tap` dependency and no test script (F-243), so the
equivalent evidence lives in `core/semantic-log` (the flow tests assert the region span; the diagram
unit tests assert the block) and in `core/blong-gogo`'s `SemanticLog.test.ts` (a framework record
carries the branch and the points). Revisit only if this package gets a working test entry.
